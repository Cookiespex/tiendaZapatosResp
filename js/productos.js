//Variables
const carrito = document.querySelector('#carrito');
const productos = document.getElementById('lista-productos');
const infoProducto = document.querySelectorAll('#lista-productos .producto .info-producto');

//Event Listener
cargarEventListener();

function cargarEventListener() {
    infoProducto.forEach(producto => {
        /*producto.addEventListener('mouseover', elejirItem)*/
    })
}
